# Three.js Scroll Card Rail

Паттерн для создания горизонтальной карусели карточек с Three.js.

## Использование
См. `main.js` — функция `createCardRail()`.

## Ключевые моменты
- Использует `ScrollTrigger` от GSAP
- Карточки анимируются при скролле
- Поддерживает 3D трансформации
